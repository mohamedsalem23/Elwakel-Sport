# 🚀 دليل التشغيل السريع - إلواكل سبورت

## البدء السريع

### 1️⃣ تشغيل Backend

```bash
cd backend
python main.py
```

**النتيجة المتوقعة:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Created default admin user: admin/Admin@123
```

✅ Backend جاهز على `http://localhost:8000`

---

### 2️⃣ تشغيل Frontend

```bash
cd frontend
npm run dev
```

**النتيجة المتوقعة:**
```
> next dev
ready - started server on 0.0.0.0:3000
```

✅ Frontend جاهز على `http://localhost:3000`

---

## 🔐 بيانات الدخول الافتراضية

### حساب Admin
```
Username: admin
Password: Admin@123
```

### تسجيل مستخدم عادي
انقر على **"اشترك الآن"** وأنشئ حساب جديد

---

## 🎯 سير العمل

### 1. **الصفحة الرئيسية** (localhost:3000)
- تصميم محسّن مع خلفيات متحركة
- أزرار دخول واشتراك
- ميزات النظام معروضة

### 2. **تسجيل الدخول** (localhost:3000/login)
- تصميم احترافي مع Glass Morphism
- أنيمشنات سلسة
- **يوجه الأدمن تلقائياً إلى لوحة التحكم**
- يوجه المستخدمين إلى لوحة الحجز

### 3. **التسجيل** (localhost:3000/signup)
- نموذج متقدم بحقول متعددة
- تحقق من البيانات من الـ Backend
- إعادة توجيه تلقائية للدخول

### 4. **لوحة المستخدم** (localhost:3000/dashboard/user)
- تقويم حجز تفاعلي
- إدارة الحجوزات الشخصية

### 5. **لوحة الأدمن** (localhost:3000/dashboard/admin)
- إدارة جميع الحجوزات
- إحصائيات النظام
- تصفية وحذف الحجوزات

---

## 🎨 ميزات التصميم الجديد

### ✨ في جميع الصفحات:
- ✅ خلفيات متحركة (Animated Gradients)
- ✅ أشكال هندسية عائمة (Floating Blobs)
- ✅ تأثيرات Glass Morphism
- ✅ أنيمشنات سلسة على التحميل
- ✅ أزرار بتأثيرات برق
- ✅ forms مع تأثيرات التركيز

### 📱 الاستجابة المحمولة:
- ✅ جميع الصفحات متوافقة مع الهواتف
- ✅ حجم خط وأزرار مناسب على الجوال
- ✅ تخطيط ديناميكي حسب حجم الشاشة

---

## 🔧 الملفات الرئيسية

```
frontend/
├── src/
│   ├── pages/
│   │   ├── index.js          (الصفحة الرئيسية)
│   │   ├── login.js          (تسجيل الدخول)
│   │   ├── signup.js         (التسجيل)
│   │   ├── dashboard/
│   │   │   ├── user.js       (لوحة المستخدم)
│   │   │   └── admin.js      (لوحة الأدمن)
│   │
│   ├── components/
│   │   └── Navbar.js         (شريط التنقل)
│   │
│   ├── styles/
│   │   ├── globals.css       (أنماط عامة)
│   │   ├── Home.module.css   (الصفحة الرئيسية)
│   │   ├── Login.module.css  (تسجيل الدخول)
│   │   ├── Signup.module.css (التسجيل)
│   │   ├── Navbar.module.css (شريط التنقل)
│   │   └── ...

backend/
├── main.py                    (نقطة الدخول)
├── auth.py                    (المصادقة + كلمات المرور)
├── crud.py                    (عمليات قاعدة البيانات)
├── database.py                (اتصال قاعدة البيانات)
├── models.py                  (نماذج البيانات)
└── routers/
    ├── auth.py                (نقاط نهاية المصادقة)
    ├── bookings.py            (نقاط نهاية الحجوزات)
    └── admin.py               (نقاط نهاية الأدمن)
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: CORS Error
```
Access to XMLHttpRequest blocked by CORS
```
**الحل:**
- ✓ Backend يعمل على port 8000
- ✓ Frontend يعمل على port 3000
- ✓ CORS مفعل في main.py

### المشكلة: Toast/Notification لا تظهر
**الحل:**
- استخدم `console.log()` للتصحيح
- تحقق من الـ alert boxes للأخطاء

### المشكلة: الأنيمشنات بطيئة
**الحل:**
- افتح Developer Tools (F12)
- انتقل لـ Performance tab
- تحقق من FPS يجب أن يكون 60+

---

## 📊 API Endpoints

### المصادقة:
```
POST   /auth/token          - تسجيل الدخول
POST   /auth/users          - إنشاء مستخدم
GET    /auth/users/me       - بيانات المستخدم الحالي
```

### الحجوزات:
```
GET    /bookings            - جميع الحجوزات
POST   /bookings            - إنشاء حجز
DELETE /bookings/{id}       - حذف حجز
```

### الأدمن:
```
GET    /admin/bookings      - جميع الحجوزات (للأدمن)
DELETE /admin/bookings/{id} - حذف حجز (للأدمن)
```

---

## 💡 نصائح مهمة

1. **في حالة عدم عمل الأدمن:**
   - تأكد من أن كلمة المرور صحيحة: `Admin@123`
   - قد تحتاج لإعادة تشغيل Backend لإنشاء الحساب الافتراضي

2. **لحذف قاعدة البيانات:**
   ```bash
   rm backend/elwakel.db
   # ثم أعد تشغيل Backend
   ```

3. **للتعديل على CSS:**
   - عدّل الملفات في `frontend/src/styles/`
   - المتصفح سيحدث تلقائياً (Hot Reload)

4. **للتعديل على Backend:**
   - عدّل الملفات في `backend/`
   - أعد تشغيل البرنامج يدويًا

---

## ✅ Checklist التشغيل

- [ ] Backend يعمل بدون أخطاء
- [ ] Frontend يحمل الصفحة الرئيسية
- [ ] الأنيمشنات تعمل بسلاسة
- [ ] يمكن التسجيل بحساب جديد
- [ ] يمكن تسجيل الدخول كمستخدم
- [ ] يمكن تسجيل الدخول كأدمن
- [ ] الأدمن يُعاد توجيهه تلقائياً
- [ ] الأزرار والنماذج تعمل
- [ ] لا توجد أخطاء في Console

---

## 🎉 مبروك!

تم تحضير نظام **إلواكل سبورت** بنجاح!

**استمتع بتجربة نظام الحجز الاحترافي!** ⚽
2. أعد تشغيل البرنامج
3. امسح ذاكرة المتصفح (Ctrl+Shift+Delete)
4. اقرأ الأخطاء في console (F12)

---

## 📚 معلومات إضافية:

- **دليل التثبيل**: INSTALLATION_GUIDE_AR.md
- **حالة المشروع**: PROJECT_STATUS.md  
- **تقرير الإكمال**: COMPLETION_REPORT.md
- **قراءة كاملة**: README_AR.md

---

**استمتع! ⚽**
